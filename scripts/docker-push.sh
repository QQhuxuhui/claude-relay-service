#!/bin/bash

# ============================================================================
# Docker 快速构建并推送脚本
# ============================================================================
# 功能：一键构建并推送镜像到阿里云
# 使用：./scripts/docker-push.sh
# ============================================================================

set -e

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# 调用主构建脚本，启用推送
"${SCRIPT_DIR}/docker-build.sh" --push "$@"
